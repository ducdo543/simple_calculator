
-- more like a function because it has one method return the result,
-- use class because familiar and take advantage of self
GiveResult = Class{}

function GiveResult:init(values_pressed)
    self.values_pressed = values_pressed
end

function GiveResult:calculate()

end



